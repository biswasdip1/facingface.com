# FacingFace Fix Notes — 22 May 2026

This package includes the following fixes:

1. Fixed the post creation `bgColor` validation error by ensuring saved background color values are short CSS color values that satisfy the server/PostgreSQL 30-character limit. Gradient previews are still preserved in the UI where applicable.
2. Increased the post text capacity from 3,000 words to 5,000 words.
3. Kept the existing post text box size unchanged, as requested.
4. Updated the create-post counter to show word count against the shared post word limit.
5. For posts using a chosen background, doubled the visible collapsed text limit from 20 words to 40 words.
6. Moved the `See More` / `See Less` control outside the chosen-background text box so it no longer appears as part of the post heading/message.
7. Rebuilt the production `dist` output after the changes.

Validation completed with `pnpm run check` and `pnpm run build`.
